# TODO

## 작업 목적
- 차트에서 시리얼 번호 기준으로 성적서 상세 정보를 빠르게 조회할 수 있게 한다.
- 운영 환경에서 남아 있는 후속 구현 항목만 관리한다.

## 해야 할 일
- 오전 구현/검증 완료
  - 차트 공용 성적서 상세 모달 조회
    - `serialNo` 기준 성적서 상세 백엔드 조회 API 구현
    - 성적서 상세 모달 UI 구현 및 차트 점 클릭 연동
    - 치수번호/기준값/상하한공차/측정값/일시 형식 정리
    - 15행 기준 높이와 스크롤 이동 버튼 UX 적용
  - 차트 공용 시리얼 검색 진입점 추가
    - 메인/월/일 차트 검색 버튼 배치
    - 프로젝트 범위 시리얼 후보 검색 API 및 디바운스 검색 구현
    - 메인 차트에서 특성치 선택 없이 검색 가능하도록 차트-모달 분리
    - 검색 모달 버튼 제거, 바깥 클릭 닫기, 인풋 우측 돋보기 실행 UX 적용
  - 검증 완료
    - 백엔드 `ReportControllerMockMvcTest`, `ReportServiceSerialDetailIntegrationTest` 통과
    - 프론트 `npm run build`, `serialReportDetailScroll.test.js` 통과

- CPK 기반 원형 40세그먼트 추론 기능 브레인스토밍/사전 설계
  - 입력값을 `projId`, 기준 `serialNo`, `charNo`, 목표 `cpk`, 최종 표본 수로 정의하고 API 스펙 초안 작성
  - SQL은 `projId + charNo` 기준 후보 `(serialNo, measuredValue, inspDt, inspReportId)` 조회까지만 맡기고, 조합 탐색은 Java 서비스에서 수행하는 구조 검토
  - 기준 측정값 1개를 반드시 포함한 상태에서 나머지 표본을 선택하는 탐색 알고리즘 후보 비교
  - 정확 일치 대신 "목표 CPK를 만족하거나 가장 근접한 조합"을 찾는 휴리스틱/빔서치 방식 검토
  - 목표 CPK를 만족하는 조합을 찾으면 `(serialNo, measuredValue)` 리스트 반환, 못 찾으면 실패 메시지 반환하는 응답 정책 정리
  - 후보군 범위를 같은 프로젝트 전체 이력으로 둘지, 배치/기간/공정 조건으로 좁힐지 내일 확인 후 확정

- 운영 후속 점검
  - 운영 환경 900건 수준 업로드 체감 시간과 DB 부하 재계측
  - preview 샘플 건수와 batch flush 크기 운영값 튜닝

## 참고 메모
- 시리얼 검색 기능은 차트 화면 전환과 분리하고, `serialNo`만으로 성적서 상세 모달을 직접 여는 방향으로 정리했다.
- CPK 역산은 단순 조회가 아니라 조합 탐색 문제에 가깝다. SQL만으로 풀기보다 후보군 조회는 SQL, CPK 계산과 최적 조합 선택은 Java 서비스에서 처리하는 방향이 현실적이다.
- 목표 CPK 정확 일치가 필수가 아니므로, 탐색 결과가 기준치 이상이면 반환하고 기준치 미달이면 검색 실패로 처리하는 정책이 적합하다.

